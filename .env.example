# Evento Web — Public Pages (.env.local for development)

# Backend API base URL (no trailing slash)
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Your app's main URL (used in RSVP login redirect and LoginPrompt buttons)
VITE_APP_URL=https://app.evento.iq
